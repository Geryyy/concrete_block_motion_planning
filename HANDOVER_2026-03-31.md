## Motion Planning Handover - 2026-03-31

Current branch: `main`

Checkpoint intent:
- keep the simplified prototype-style motion planning setup
- preserve the current demo/debug state so work can resume quickly

What changed in this checkpoint:
- simplified package toward one prototype planner path
- stored resolved crane URDF at `motion_planning/data/crane.urdf`
- added live-seed plumbing for start-state equilibrium seeding
- added `--start-only` to `motion_planning.demo`
- improved demo axis fitting
- added K8 frame triad drawing in demo
- simplified crane arm approximation in `motion_planning/geometry/arm_model.py`

Current known issues:
- passive steady-state convergence is still poor for some states; large residual warnings are real
- moving block size in the demo looks wrong
- crane arm capsule approximation is still not aligned correctly with the visible crane
- TCP/K8 frame looks flipped; `K8_tool_center_point` / tool collision chain likely has a 180 deg issue

Most recent user feedback to continue from:
- "block size is wrong"
- "crane arm approximation is not correct"
- "tcp has z axis up"

Useful commands:
```bash
cd /workspaces/ros2_baustelle_ws/src/concrete_block_stack/concrete_block_motion_planning
python3 -m motion_planning.demo --scenario step_02_second_beside_first
python3 -m motion_planning.demo --scenario step_02_second_beside_first --start-only
```

Notes:
- current live TF/joint-state probing was used to improve start seeding
- the stored URDF was added because we will likely need to inspect frames/collision geometry again
