\documentclass[11pt,a4paper]{article}
\usepackage{amsmath,amssymb,geometry,bm}
\geometry{margin=2.5cm}

\newcommand{\R}{\mathbb{R}}
\newcommand{\q}{\bm{q}}
\newcommand{\dq}{\dot{\bm{q}}}
\newcommand{\ddq}{\ddot{\bm{q}}}

\title{Cartesian Path Following OCP (Free-Time, acados)}
\author{}
\date{}

\begin{document}
\maketitle

\section*{State, Control, and Time Scaling}
We use normalized horizon time $\tau\in[0,1]$ and optimize the physical final time $T>0$.
\begin{align}
\bm{x}(\tau) &= \begin{bmatrix}\q(\tau)\\ \dq(\tau)\\ s(\tau)\\ \dot s(\tau)\\ T\end{bmatrix}\in\R^{2n_v+3},
\qquad
\bm{u}(\tau)=\begin{bmatrix}\ddq_a(\tau)\\ v(\tau)\end{bmatrix}\in\R^{n_a+1},
\\
\frac{dt}{d\tau} &= T.
\end{align}
The continuous dynamics are scaled as
\begin{align}
\q' &= T\,\dq, \\
\dq' &= T\,\ddq(\q,\dq,\ddq_a), \\
 s' &= T\,\dot s, \\
 \dot s' &= T\,v, \\
 T' &= 0,
\end{align}
where prime denotes $d/d\tau$.

\section*{Path and Tracking Error}
The Cartesian reference is a spline $\bm{r}(s)\in\R^3$. TCP position is $\bm{p}_{tcp}(\q)$.
\begin{equation}
\bm{e}_{xyz}(\q,s)=\bm{p}_{tcp}(\q)-\bm{r}(s).
\end{equation}

\section*{Running Cost (NONLINEAR\_LS)}
With Gauss-Newton least squares, stage residual is
\begin{equation}
\bm{y}(\tau)=\begin{bmatrix}
\sqrt{T}\,\bm{e}_{xyz} \\
\sqrt{T}\,s \\
\sqrt{T}(\dot s-\dot s_{ref}) \\
\sqrt{T}\,\ddq_a \\
\sqrt{T}\,v \\
\sqrt{T}(\q_p-\q_{p,eq}) \\
\sqrt{T}\,\dq_p \\
T
\end{bmatrix},
\qquad
\ell(\tau)=\tfrac12\|\bm{y}(\tau)-\bm{y}_{ref}(\tau)\|_{W}^2.
\end{equation}
The progress reference is stage-varying:
\begin{equation}
 s_{ref}(\tau_k)=s_0+(1-s_0)\tau_k,
\end{equation}
so progress is distributed over the full horizon and does not saturate early.

\section*{Terminal Cost and Constraints}
Terminal residual:
\begin{equation}
\bm{y}_e=\begin{bmatrix}
\bm{e}_{xyz} \\
\dq \\
1-s \\
\dot s \\
\q_p-\q_{p,eq} \\
\dq_p
\end{bmatrix},
\qquad
\ell_e=\tfrac12\|\bm{y}_e\|_{W_e}^2.
\end{equation}
Hard terminal constraints used in the implementation:
\begin{equation}
\dq(1)=0,\qquad \dot s(1)=0,\qquad s(1)=1.
\end{equation}

\section*{Bounds}
\begin{align}
T_{min} \le T \le T_{max},\quad
s_{min}\le s\le s_{max},\quad
\dot s_{min}\le\dot s\le\dot s_{max},
\end{align}
with joint position/acceleration bounds from URDF and YAML overrides.

\section*{Discretization and Solver}
Direct multiple shooting with $N$ intervals over $\tau\in[0,1]$:
\begin{equation}
\Delta\tau=\frac{1}{N},\qquad t_k=T^*\tau_k.
\end{equation}
acados settings: IRK integrator, SQP NLP, partial condensing HPIPM QP.

\end{document}
